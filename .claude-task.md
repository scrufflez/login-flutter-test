# Task: Update Flutter Login App to Asgard Theme

Update the Flutter login app at /Users/brentperry/Developer/login_flutter_test/ to use the Asgard theme defined below.

## Files to modify

### lib/main.dart
Replace the entire file. Keep the SAME login logic (password "pa55w0rd" succeeds, anything else fails) but completely re-theme the UI.

### pubspec.yaml
Add dependencies:
- google_fonts (for Inter font)
- lucide_icons (for icons)

## Asgard Theme Spec

### Colors
```dart
// Light mode
AsgardColors.light(
  background: Color(0xFFF8FAFC),
  surface: Color(0xFFFFFFFF),
  primary: Color(0xFF1E40AF),
  primaryHover: Color(0xFF1E3A8A),
  primaryLight: Color(0xFF3B82F6),
  success: Color(0xFF10B981),
  warning: Color(0xFFF59E0B),
  danger: Color(0xFFEF4444),
  info: Color(0xFF0EA5E9),
  border: Color(0xFFE2E8F0),
  textPrimary: Color(0xFF0F172A),
  textSecondary: Color(0xFF64748B),
)

// Dark mode
AsgardColors.dark(
  background: Color(0xFF0F172A),
  surface: Color(0xFF1E2937),
  primary: Color(0xFF3B82F6),
  ...
  border: Color(0xFF334155),
  textPrimary: Color(0xFFF1F5F9),
  textSecondary: Color(0xFF94A3B8),
)
```

### Typography
Font: Inter (via google_fonts)
- H1: 32px w700
- H2: 24px w600
- H3: 20px w600
- Body: 16px w400
- Body Small: 14px w400
- Caption: 12px w500
- Button: 15px w600

### Design Tokens
- Border radius: 8, 12, 16, 20
- Spacing base: 8.0
- Soft card shadows
- Support BOTH light and dark mode

### Login Screen Requirements
The login screen should look like a premium SaaS app with:
- A centered card layout (max ~400px wide)
- AsgardPrimaryButton styled button
- AsgardTextField styled inputs with lucide_icons prefixes
- Clean "Sign In" heading (H2 style)
- Error message shown in a styled AsgardBadge-like container
- Support dark mode based on system preference
- App title "Asgard" in the top-left area
- A tagline or subtitle

### Welcome Screen (after login)
- Show "Welcome, [username]!" with Asgard styling
- A sign-out button
- Dark mode support

### Important
- Keep the password logic EXACTLY as-is: password "pa55w0rd" succeeds, anything else fails
- The app MUST support both light and dark mode automatically
- Use `flutter_lints` for linting
- Keep sound null safety
- Keep the project name as login_flutter_test

## After updating files
1. Run `flutter pub get` to install new deps
2. Run `flutter build web --web-renderer html --release` to rebuild  
3. Verify build/web/ is created successfully
