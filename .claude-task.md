# Task: Flutter Web Login App

Create a complete Flutter web application in /Users/brentperry/Developer/login_flutter_test/ with the following requirements:

## Project Setup
- Initialize a Flutter web project in the existing directory /Users/brentperry/Developer/login_flutter_test/
- Create pubspec.yaml with web support
- The project name is "login_flutter_test"
- Use the existing directory structure (lib/, web/, test/ already exist)

## Login Screen (lib/main.dart)
- A simple, clean login screen built with Flutter widgets
- Two text fields: Username and Password
- A "Login" button
- Hardcoded password check: if password == "pa55w0rd" then login succeeds
  - On success: show a success message or navigate to a simple "Welcome, [username]!" page
  - On failure: show a "Login failed. Invalid credentials." error message
- Any other password fails with login error
- Username can be anything, only the password is validated

## Web Support
- Create web/index.html with the standard Flutter web bootstrap
- Create web/manifest.json
- Ensure the app builds for web (flutter build web)

## Vercel Compatibility
- Create a vercel.json in the project root for Flutter web deployment:
  - Output directory should be "build/web" (Flutter's default web build output)
  - Framework should be set appropriately
  - Include a rewrite rule to handle Flutter's client-side routing (all routes to index.html)

## After creating all files
1. Build the project with: flutter pub get
2. Build with: flutter build web --release
3. Verify the build/web directory is created successfully

## IMPORTANT
- Use `flutter pub get` after creating pubspec.yaml
- Build: `flutter build web --release`
- All files go in /Users/brentperry/Developer/login_flutter_test/

The flutter SDK is at /usr/local/Caskroom/flutter/2.5.0/flutter/ which is very old (2021). For web support in this version, you may need to enable it with `flutter config --enable-web` first, or you may need to manually configure web support. Focus on creating the correct file structure that would work.

Actually, since flutter 2.5.0 is too old for proper web support, create the project as a standard Flutter project and manually add web support files. The key files needed:
- pubspec.yaml with flutter SDK dependency
- lib/main.dart with the login app
- web/index.html (Flutter web entry point)
- web/manifest.json
- analysis_options.yaml
- vercel.json

Don't worry if flutter build web fails - just get the code right and ensure the Vercel config is correct.
