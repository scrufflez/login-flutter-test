# Task: Rewrite main.dart with Asgard 1.4 Theme (Panago Implementation)

## Context
You are in `/Users/brentperry/Developer/login_flutter_test/` — a Flutter 2.5 web application with a login screen.

## Requirements

Rewrite `lib/main.dart` to implement the **Asgard 1.4 Theme** exactly as specified below. This is a **dark-only** industrial theme — no light mode. Do NOT change the auth logic (password `pa55w0rd` → welcome, anything else → error badge).

**CRITICAL: Flutter 2.5 API constraints** — Use `primary`/`onPrimary` NOT `backgroundColor`/`foregroundColor` in `ElevatedButton.styleFrom`. `primary` is the Flutter 2.5 property name.

## Detailed Spec

### 1. Color Palette (dark only, no light mode)

```dart
class AsgardColors {
  // Backgrounds
  static const Color backgroundDeep = Color(0xFF0F1113);
  static const Color background = Color(0xFF151819);       // main page bg
  static const Color topBar = Color(0xFF0B0D0E);           // top navigation
  static const Color surface = Color(0xFF1A1F23);          // cards / inputs
  static const Color surfaceRaised = Color(0xFF20262B);    // menus / raised panels
  static const Color border = Color(0xFF2A3036);           // subtle borders
  static const Color borderStrong = Color(0xFF374151);     // inputs / active surfaces

  // Brand / Action
  static const Color primaryBlue = Color(0xFF5DB7F6);
  static const Color primaryBlueHover = Color(0xFF3B9FE8);
  static const Color primaryBlueMuted = Color(0xFF2F80C7);
  static const Color focusBlue = Color(0xFF60A5FA);

  // Text
  static const Color textPrimary = Color(0xFFE5E7EB);
  static const Color textSecondary = Color(0xFF9CA3AF);
  static const Color textMuted = Color(0xFF3F444A);
  static const Color textBrand = Color(0xFF5DB7F6);        // PANAGO logo color
  static const Color textDisabled = Color(0xFF5F6670);

  // Semantic
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color danger = Color(0xFFEF4444);
  static const Color info = Color(0xFF0EA5E9);
}
```

### 2. ThemeData (dark only)

```dart
ThemeData(
  brightness: Brightness.dark,
  scaffoldBackgroundColor: Color(0xFF151819),
  primaryColor: Color(0xFF5DB7F6),
  colorScheme: const ColorScheme.dark(
    primary: Color(0xFF5DB7F6),
    surface: Color(0xFF1A1F23),
    background: Color(0xFF151819),
    error: Color(0xFFEF4444),
  ),
  appBarTheme: const AppBarTheme(
    backgroundColor: Color(0xFF0B0D0E),
    elevation: 0,
    centerTitle: false,
  ),
)
```

### 3. Text Styles (Inter font via google_fonts)

| Role | Size | Weight | Color |
|------|------|--------|-------|
| Brand (PANAGO) | 18px | 600 | textBrand |
| Nav Item | 13px | 600 | textSecondary |
| H1 | 34px | 400 | textMuted (sparse heading, low contrast) |
| H2 | 24px | 600 | textPrimary |
| Body | 15px | 400 | textSecondary |
| Caption | 12px | 500 | textMuted |
| Button | 13px | 700 | varies (uppercase) |

### 4. AsgardTopNav

Full-width top bar positioned at top of every screen.

```dart
AsgardTopNav (
  height: 58,
  backgroundColor: Color(0xFF0B0D0E),
  borderBottom: 1px solid Color(0xFF202426)
)
```

Layout:
```
PANAGO (blue, uppercase, left-aligned)  ....  SearchIcon  Menu(circle)  LOG IN(outlined)  SIGN UP(solid blue)  CartIcon
```

- **PANAGO**: Text style: fontSize 18, fontWeight 600, color #5DB7F6, letterSpacing 1.2
- **Search icon**: LucideIcons.search, color #6B7280, size 18
- **Menu**: circle container (32x32, bg: Color(0xFF222427)) with LucideIcons.menu, size 16
- **LOG IN**: outlined button. height 34, borderRadius 22, border Color(0xFF2A3036), text color #5DB7F6, fontSize 13, fontWeight 700, letterSpacing 1.1
- **SIGN UP**: solid button. height 34, borderRadius 22, bg #5DB7F6, text color #071014, fontSize 13, fontWeight 700, letterSpacing 1.1
- **Cart icon**: LucideIcons.shoppingCart, color #6B7280, size 18

For the login screen specifically, the top nav should still be present.

### 5. Login Screen

- Background: AsgardColors.background (#151819)
- Top nav present at top
- Centered card (maxWidth: 420)
- Card: AsgardColors.surface (#1A1F23) bg, 1px border AsgardColors.border, borderRadius 10, NO shadow
- Inside card: "Sign In" heading (H2: 24px, 600, textPrimary), tagline "Access your workspace" (14px, textSecondary)
- Username field + Password field
- Error badge (if login fails)
- "SIGN IN" button (full width, solid blue per AsgardPrimaryButton spec)

### 6. AsgardTextField spec

```dart
fillColor: Color(0xFF1A1F23)
borderColor: Color(0xFF2A3036)
focusedBorderColor: Color(0xFF60A5FA)
textColor: Color(0xFFE5E7EB)
placeholderColor: Color(0xFF6B7280)
borderRadius: 8
prefixIcon: Color(0xFF6B7280)
```

### 7. AsgardPrimaryButton spec

```dart
backgroundColor: Color(0xFF5DB7F6)
foregroundColor: Color(0xFF071014)  // dark text on blue
borderRadius: 22  // pill-shaped
height: 34
fontWeight: FontWeight.w700
letterSpacing: 1.1
fontSize: 13
```

No gradient, no glow, no shadow.

### 8. AsgardLoginButton (outlined button variant)

```dart
backgroundColor: Colors.transparent
foregroundColor: Color(0xFF5DB7F6)
borderColor: Color(0xFF2A3036)
borderRadius: 22
height: 34
fontSize: 13
fontWeight: 700
letterSpacing: 1.1
```

### 9. AsgardErrorBadge

```dart
bg: Color(0xFFEF4444).withOpacity(0.1)
border: Color(0xFFEF4444).withOpacity(0.3)
text: Color(0xFFEF4444), size 13, weight 500
icon: LucideIcons.alertCircle, size 16
borderRadius: 8
```

### 10. Welcome / Home Screen

After successful login, show a "home" screen with:
- AsgardTopNav at top
- Main content area: centered, muted "Home Page" text (H1: 34px, 400, textMuted — low contrast, sparse)
- A small "Sign out" button (outlined, AsgardLoginButton style but with LucideIcons.logOut)

### 11. Auth Logic (UNCHANGED)

```dart
if (password == 'pa55w0rd') {
  // navigate to WelcomeScreen/HomeScreen
} else {
  // show AsgardErrorBadge with "Invalid credentials."
}
```

### 12. General visual rules

- NO gradients anywhere
- NO glow effects
- NO box shadows on cards/surfaces (use borders only)
- Cards use borders instead of elevation
- Typography is restrained — content text quiet
- Empty-state / placeholder text is muted
- Layout feels sparse and intentional
- Blue used only for CTAs, links, focus, and selected state

## Output Format

Write the complete file to `lib/main.dart`. It should be a single self-contained file containing all classes (it's okay to have everything in one file for this Flutter 2.5 app). Import:
- `package:flutter/material.dart`
- `package:google_fonts/google_fonts.dart`
- `package:lucide_icons/lucide_icons.dart`

## Verification

After writing the file, run:
```bash
flutter pub get
flutter build web --web-renderer html --release
```

Both must succeed with exit code 0. Fix any compile errors.
